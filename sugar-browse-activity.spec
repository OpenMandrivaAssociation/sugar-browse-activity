# NOTE: Do not edit, file was generated by jhconvert

Name: sugar-browse-activity
Version: 102
Release: %mkrel 1
Summary: Browse activity for Sugar
License: GPLv2+
Group: Graphical desktop/Other
Url: http://sugarlabs.org/

# wget --no-clobber --continue 'http://git.sugarlabs.org/projects/browse/repos/mainline/archive/89b77a96f594a83a86ad3981c310e00c68e2387f.tar.gz' -O sugar-browse-activity-102-20081229.tar.gz
Source: sugar-browse-activity-102-20081229.tar.gz

Requires: python-hulahop >= 0.4.8
Requires: sugar-toolkit >= 0.83.3
Requires: python  
Requires: python-cjson  

BuildRequires: sugar-toolkit >= 0.83.3
BuildRequires: libpython-devel  
BuildRequires: gettext  

BuildRoot: %{_tmppath}/%{name}-%{version}-%{release}-root-%(%{__id_u} -n)
BuildArch: noarch

%description
Browse is a Web browser built on Xulrunner and thus uses the same
Gecko rendering engine as Firefox.

%prep
%setup -q -n browse-mainline


%build
python  \
	setup.py \
	build

%install
rm -rf %{buildroot}
[ -f setup.py ] && chmod 0755 setup.py
python  \
	setup.py \
	install \
	--prefix=%{buildroot}/%{_prefix}
%find_lang org.laptop.WebActivity

%clean
rm -rf %{buildroot}

%files -f org.laptop.WebActivity.lang
%defattr(-,root,root,-)
%{_datadir}/sugar/activities/*
%doc AUTHORS COPYING

